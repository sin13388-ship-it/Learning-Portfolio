# 筆記生成 Prompt 模板

這份檔案專門放你可複用的 prompt，目標是產出和你目前喜歡的「有系統、例題風格、可直接複習」筆記。

---

## 1) 通用版（任何語言）

```text
你是一位程式教學助教。請把我提供的課堂程式碼與主題，整理成「有系統、例題導向」的學習筆記。

【輸出語言】繁體中文
【目標讀者】初學者到中階
【風格】條理清楚、精簡但完整、可直接拿來複習考試

請遵守以下格式：
1. 文件標題：<語言 + 主題> 重點筆記
2. 章節使用編號（1, 2, 3...），每章含：
   - 核心概念（2~5 點）
   - 對照表（若適合）
   - 最小可執行範例
   - 常見誤區 / 注意事項
3. 至少加入 1 個「例題章節」：
   - 題目背景
   - 完整程式碼
   - 執行流程說明
   - 預期輸出
   - 核心語法對照表
4. 文件結尾要有「快速總結」：
   - 用樹狀文字整理觀念
5. 用 Markdown 輸出
6. 程式碼區塊加語言標籤
7. 內容必須貼合我提供的程式碼，不要泛泛而談

額外要求：
- 遇到容易混淆的觀念（例如值比較 vs 參考比較）要用一句話對照。
- 每章標題要短且具體。
- 若有 API/語法（如 name、ordinal、copy、equals），要補「這個屬性/方法是做什麼」的一句解釋。

以下是我的課堂內容：
<在這裡貼上程式碼或重點>
```

---

## 2) Kotlin 專用版

```text
請把我的 Kotlin 課堂程式整理成「重點筆記 + 例題」格式。

必含章節：
1. data class 核心（toString / equals / hashCode / copy）
2. == / === 差異
3. copy 是 shallow copy 的說明
4. object singleton 的用途
5. enum class（基本列舉、帶參數列舉、name、ordinal、when）
6. 一個完整例題（例如 StudentManager）
7. 快速總結（樹狀）

輸出規則：
- 繁體中文
- Markdown
- 每章都要有最小範例
- 有比較就用表格
- 內容要對應我的程式碼，不可空泛

我的 Kotlin 程式碼：
<貼上 Kotlin 程式碼>
```

---

## 3) Java 專用版

```text
請把我的 Java 課堂程式整理成「重點筆記 + 例題」格式。

必含章節（可依主題微調）：
1. 類別與物件
2. equals() vs ==
3. static（若有）
4. enum + switch
5. 集合操作（List/Map）
6. 一個完整例題（新增、查詢、刪除流程）
7. 快速總結（樹狀）

輸出規則：
- 繁體中文
- Markdown
- 每章都要有最小可執行範例
- 易混淆觀念要做對照表
- 依我提供的程式碼整理，不加無關框架

我的 Java 程式碼：
<貼上 Java 程式碼>
```

---

## 4) Python 專用版

```text
請把我的 Python 課堂程式整理成「重點筆記 + 例題」格式。

必含章節（可依主題微調）：
1. 資料模型（class / dataclass）
2. 值比較（==）與身分比較（is）
3. 可變與不可變資料（list/dict/tuple）
4. Enum（若有）與 match/case 或 if-elif
5. 清單/字典操作與迴圈
6. 一個完整例題（新增、顯示、刪除）
7. 快速總結（樹狀）

輸出規則：
- 繁體中文
- Markdown
- 程式碼可直接執行（Python 3）
- 比較或差異請用表格
- 內容必須對應我的程式碼

我的 Python 程式碼：
<貼上 Python 程式碼>
```

---

## 5) C 專用版

```text
請把我的 C 課堂程式整理成「重點筆記 + 例題」格式。

必含章節（可依主題微調）：
1. struct / enum 基礎
2. 指標與記憶體（值傳遞 vs 位址傳遞）
3. 陣列與字串處理
4. 函式拆分與流程控制（switch）
5. 一個完整例題（新增、顯示、刪除或查找）
6. 常見錯誤（越界、未初始化、字串處理）
7. 快速總結（樹狀）

輸出規則：
- 繁體中文
- Markdown
- 範例使用 C11，可用 gcc 編譯
- 重要觀念務必加「注意事項」
- 內容要貼合我提供的程式碼

我的 C 程式碼：
<貼上 C 程式碼>
```

---

## 6) 一鍵強化版（你想要更像考前講義時用）

```text
請在原有筆記結構外，額外加入：
1. 每章 2 題小測驗（附答案）
2. 一份「考前速記表」（最多 20 行）
3. 3 個常見陷阱題（附錯誤原因）
```

---

## 7) 使用方式（建議）

1. 先貼「通用版」或語言專用版 prompt。
2. 再貼老師最新程式碼。
3. 若要更完整，最後加上「一鍵強化版」。
4. 若輸出太長，可補一句：
   - `請先輸出章節大綱，確認後再展開全文。`

---
【通用】
把我提供的程式碼整理成繁中 Markdown「重點筆記 + 例題」：
1) 概念重點
2) 關鍵語法範例
3) 常見陷阱
4) 1 個完整例題（流程+預期輸出）
5) 最後用樹狀快速總結
   內容必須對應我的程式碼，不要空泛。